# Firebase/Gemini AI Configuration
# The GoogleService-Info.plist handles most Firebase auth automatically
# Only needed if using Google AI (Gemini API) directly instead of Vertex AI

# Optional: Google AI API Key (only if using GoogleAIBackend without Firebase)
# EXPO_PUBLIC_GOOGLE_AI_API_KEY=your_api_key_here

# Optional: Custom system instructions for the coach
# EXPO_PUBLIC_COACH_SYSTEM_PROMPT="You are a supportive accountability coach..."
